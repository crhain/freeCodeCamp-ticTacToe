/*Handles all game logic and coordination between game interface elements*/
var board = board || {};

var game = (function(board, controlPanel, messageWindow){



})(board, control, message);

console.log(board);
